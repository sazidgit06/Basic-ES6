/**
 * Write an arrow function that will take 3 parameters,
 * will multiply the parameters and will return the result.
 */


const mul = (num1, num2, num3) => num1*num2*num3;
const res = mul(1, 2, 3);
console.log(res);